const basePlaceholderURL = 'https://jsonplaceholder.typicode.com/';

export const placeholderUrls = {
    users: '/users',
    posts: '/posts',
    comments: '/comments'
}

export default basePlaceholderURL
